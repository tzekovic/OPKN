<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title : 'Book Exchange' %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header>
        <div class="container">
            <nav class="nav-container">
                <a href="/" class="logo">BookExchange</a>
                
                <ul class="nav-links">
                    <% if (user) { %>
                        <% if (user.role === 'admin') { %>
                            <li><a href="/admin/dashboard" class="nav-link">Dashboard</a></li>
                        <% } else if (user.role === 'seller') { %>
                            <li><a href="/seller/books" class="nav-link">My Books</a></li>
                            <li><a href="/seller/orders" class="nav-link">Orders</a></li>
                            <li><a href="/seller/profile" class="nav-link">Profile</a></li>
                        <% } else { %>
                            <li><a href="/search" class="nav-link">Search</a></li>
                            <li><a href="/buyer/cart" class="nav-link">Cart</a></li>
                            <li><a href="/buyer/orders" class="nav-link">Orders</a></li>
                            <li><a href="/buyer/profile" class="nav-link">Profile</a></li>
                        <% } %>
                        
                        <li style="position: relative;">
                            <a href="/chat/inbox" class="nav-link">Messages</a>
                            <% if (typeof unreadCount !== 'undefined' && unreadCount > 0) { %>
                                <span class="badge">
                                    <%= unreadCount %>
                                </span>
                            <% } %>
                        </li>

                        <li><a href="/auth/logout" class="btn btn-sm">Logout</a></li>
                    <% } else { %>
                        <li><a href="/auth/login" class="nav-link">Login</a></li>
                        <li><a href="/auth/register" class="btn btn-sm">Register</a></li>
                    <% } %>
                </ul>
            </nav>
        </div>
    </header>
    <main class="container py-xl">
